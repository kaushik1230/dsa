string reverseWord(string str){
    
  //Your code here
  int i,j;
  for( i=0, j=str.length()-1;i<str.length() && i<=j;i++,j--)
  {
      char temp=str[i];
      str[i]=str[j];
      str[j]=temp;
  }
  return str;
}
